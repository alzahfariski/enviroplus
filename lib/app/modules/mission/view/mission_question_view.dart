import 'package:flutter/material.dart';

class MissionQuestionView extends StatelessWidget {
  const MissionQuestionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
