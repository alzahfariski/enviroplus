import 'package:flutter/material.dart';

class TColors {
  // App theme colors
  static const Color primary = Color(0xFFA2ED3A);
  static const Color secondary = Color(0xFFFFE24B);
  static const Color accent = Color(0xFF1F89D5);

  // Text colors
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color textWhite = Colors.white;

  // Background colors
  static const Color light = Color(0xFFF6F6F6);
  static const Color dark = Color(0xFF192126);
  static const Color primaryBackground = Color(0xFF192126);

  // Background Container colors
  static const Color lightContainer = Color(0xFFF6F6F6);
  static const Color darkContainer = Color(0xFF293038);

  // Neutral Shades
  static const Color black = Color(0xFF232323);
  static const Color grey = Color(0xFFE0E0E0);
  static const Color white = Color(0xFFFFFFFF);
}
